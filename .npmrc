# Pin the public npm registry so `npm install` doesn't fall back to a machine's
# global/corp private registry (a common cause of installs hanging forever).
registry=https://registry.npmjs.org/
fund=false
audit=false
